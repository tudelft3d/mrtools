[project]
name = "mrarea"
version = "0.1.0"
description = "Calculate roof areas for CityJSON buildings"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.12.0",
]

[project.scripts]
mrarea = "mrarea.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
